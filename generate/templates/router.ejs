// templates/router.ejs
import express from 'express';
import <%= camel %>Controller from './<%= original %>.controller.js';

const router = express.Router();

router.get('/api/<%= pluralLower %>', <%= camel %>Controller.list);
router.get('/api/<%= pluralLower %>/:id', <%= camel %>Controller.get);
router.post('/api/<%= pluralLower %>', <%= camel %>Controller.create);
router.put('/api/<%= pluralLower %>/:id', <%= camel %>Controller.update);
router.delete('/api/<%= pluralLower %>/:id', <%= camel %>Controller.remove);

export default router;