// templates/router.ejs
import express from 'express';
import { read<%= pascal %>s, read<%= pascal %>, create<%= pascal %>, update<%= pascal %>, delete<%= pascal %> } from './<%= original %>.controller.js';

const router = express.Router();

router.get('/', read<%= pascal %>s);
router.get('/:id', read<%= pascal %>);
router.post('/', create<%= pascal %>);
router.put('/:id', update<%= pascal %>);
router.delete('/:id', delete<%= pascal %>);

export default router;